import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    mode: "dark",
    background: {
      default: '#303030'
    },
    primary: {
      main: '#3c3c3c',
    },
    secondary: {
      main: '#b388ff',
    }
  },
});
